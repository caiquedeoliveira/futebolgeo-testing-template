{% extends "template.njk"%}

{% block head%}
    <title>Sobre | Futebol Geográfico</title>
{% endblock %}

{% block content%}
    <div id="wrapper">
        <img src="/{{about.avatar_name}}.png" alt="Logo de {{about.title}}">
        <h1>{{about.title}}</h1>
        <h2>{{about.description}}</h2>
        <ul>
            {% for item in about.themes%}
            <li>{{item.theme}}</li>
            {% endfor %}
        </ul>
    </div>

    <div id="links" class="links-footer">
        {% for link in about.links%}
        <a href="{{link.url}}" target="_blank">{{link.name}}</a>
        {% endfor %}
    </div>
{% endblock %}